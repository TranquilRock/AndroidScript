package com.example.androidscript.Menu;

import java.util.regex.Pattern;

public class Interpreter {
    public static final String[] SUPPORTED_COMMAND = {
            "click [0-9]* [0-9]*",
            ""
    };
//Pattern.matches(SUPPORTED_FILE_NAME_PATTERN, path);
}
